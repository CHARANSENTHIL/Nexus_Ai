# Nexus AI — Backend Dependencies
# Install inside .venv: pip install -r requirements.txt

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data Validation
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Auth
pyjwt[crypto]>=2.8.0

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
aiosqlite>=0.19.0

# Cache / Queue
redis>=5.0.1

# Vector DB
chromadb>=0.4.22

# AI — LangChain + Ollama (Python 3.14 compatible)
# Note: CrewAI not compatible with Python 3.14 (regex wheel missing)
# Using LangChain AgentExecutor + create_react_agent instead
langchain>=0.2.0
langchain-core>=0.2.0
langchain-ollama>=0.1.0
langchain-community>=0.2.0
langgraph>=0.1.0


# HTTP Client
httpx>=0.26.0

# Telegram Bot
python-telegram-bot>=20.7

# OS Control (Windows)
psutil>=5.9.8
pywin32>=306; sys_platform == 'win32'
pyautogui>=0.9.54; sys_platform == 'win32'

# Vision / OCR (Docker-safe: graceful degradation if no display)
mss>=9.0.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Numerical (numpy 2.x required for Python 3.14+)
numpy>=2.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.5
pytest-mock>=3.12.0
